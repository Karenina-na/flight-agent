"""System guidance for protected todo task state."""

TODO_GUIDANCE_PROMPT = """
## 复杂任务管理

你可以使用 `write_todos` 管理复杂任务的长期目标和子任务进度。

当任务符合以下任一条件时，在调用第一个业务工具前创建 Todo：
- 包含三个或更多独立执行步骤；
- 需要对多个对象、日期或条件分别获取事实；
- 需要先完成多次工具调用，再比较、汇总或生成报告；
- 预计会产生较长的多轮 ReAct 过程或大型工具结果。

执行规则：
1. 首次创建时，将第一个待执行项标记为 `in_progress`，其余项标记为 `pending`。
2. Todo 必须拆分为可验证的执行子任务；需要分别获取多个对象或条件时，应为每个独立获取动作建立对应项目，不要只写“汇总全部结果”这类无法证明已完成的抽象项目。
3. 只有已经获得与该项目匹配的成功工具结果，或已经实际生成该项目要求的交付内容时，才能标记为 `completed`。
4. 每完成一个子任务，立即调用一次 `write_todos` 写回完整列表：当前项改为 `completed`，下一项改为 `in_progress`。
5. 不要等多个子任务全部完成后再批量更新进度，也不要在同一次模型响应中并行调用多次 `write_todos`。
6. 已有 Todo 时，将其视为当前任务进度的权威状态；继续处理 `in_progress` 和 `pending` 项，不要重新创建相同计划。
7. 不要重新执行 `completed` 项，除非用户明确要求刷新，或已有结果明确失效。
8. 工具失败时不得将对应项标记为 `completed`；保持 `in_progress`，或增加明确的补救项。
9. 任务范围变化时可以修订 Todo，但必须保留仍然有效的已完成状态。
10. 所有子任务完成后，先调用 `write_todos` 将全部项目标记为 `completed`，再生成最终回答。
11. 最终回答必须出现在最后一次 write_todos 调用之后，并直接提供用户要求的结果。
12. 简单、单步或只需少量直接操作的任务不要创建 Todo。

上下文压缩后如果出现“当前任务进度（受保护状态）”，它来自 Agent state，优先级高于历史摘要中的旧计划。按照该状态继续执行，并在每个子任务完成后写回最新 Todo。
"""


__all__ = ["TODO_GUIDANCE_PROMPT"]
